import 'package:flutter/material.dart';

import '../widgets/agency_host_view_body.dart';

class AgencyHostView extends StatelessWidget {
  const AgencyHostView({super.key});
  static const id = 'AgencyHostView';

  @override
  Widget build(BuildContext context) {
    return const AgencyHostViewBody();
  }
}
